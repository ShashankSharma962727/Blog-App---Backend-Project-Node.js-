<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('./partials/head')%>
</head>
<body class="bg-[#F5F5DC] min-h-screen">
  <%- include('./partials/nav')%>

  <% if (!user) { %>
    <h1 class="text-center text-2xl mt-20 text-gray-700">You are not logged in</h1>
  <% } else { %>
    <div class="flex justify-center items-center py-10">
      <form method="post" action="/blogs/addblog" enctype="multipart/form-data"
        class="bg-white p-6 rounded-lg shadow-md w-full max-w-md space-y-4">
        <h2 class="text-2xl font-semibold text-gray-800 text-center">Add Blog</h2>

        <div>
          <label for="title" class="block text-sm font-medium text-gray-700 mb-1">Title</label>
          <input type="text" id="title" name="title" placeholder="Enter blog title"
            class="w-full border border-gray-300 rounded-md p-2 focus:outline-none focus:ring-2 focus:ring-blue-500">
        </div>

        <div>
          <label for="body" class="block text-sm font-medium text-gray-700 mb-1">Body</label>
          <textarea id="body" name="body" rows="5" placeholder="Write your blog content..."
            class="w-full border border-gray-300 rounded-md p-2 focus:outline-none focus:ring-2 focus:ring-blue-500"></textarea>
        </div>

        <div>
          <label for="coverImageURL" class="block text-sm font-medium text-gray-700 mb-1">Upload Image</label>
          <input type="file" id="coverImageURL" name="coverImageURL" accept="image/*"
            class="w-full border border-gray-300 rounded-md p-2 cursor-pointer focus:outline-none focus:ring-2 focus:ring-blue-500">
        </div>

        <button type="submit"
          class="w-full bg-blue-600 text-white py-2 rounded-md hover:bg-blue-700 transition duration-200">
          Publish Blog
        </button>
      </form>
    </div>
  <% } %>
</body>
</html>
