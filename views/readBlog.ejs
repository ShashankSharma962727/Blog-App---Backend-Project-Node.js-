<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head')%>
</head>
<body class="bg-gray-100 min-h-screen">
    <%- include('./partials/nav')%>

    <% if(!user) { %>
        <h1 class="text-center text-2xl font-bold mt-10">You are not logged in</h1>
    <% } else { %>
        <section class="w-[90%] mx-auto bg-white mt-10 rounded-lg shadow-md p-6">
            <img src="<%= blog.coverImageURL %>" alt="Blog Image" class="w-[50%] mx-auto object-cover rounded-md mb-6">
            <h1 class="text-3xl font-semibold text-gray-800 mb-10"><%= blog.title %></h1>
            <p class="text-gray-700 leading-relaxed whitespace-pre-line"><%= blog.body %></p>
        </section>
    <% } %>
</body>
</html>